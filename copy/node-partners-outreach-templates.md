# Node Partners — outreach templates (saved)

Plain versions you used, plus merge-tag and spintax variants. **Do not** use “get” in live sends if your spam rules flag it — use *put in touch / connect / introduce* (see spintax file).

---

## 1) Mouthguards / AI outbound — **example (merge tags; no hardcoded people)**

```
{{first_name}},

We can get you in contact with sports teams that want sports mouthguards by using our AI outbound agents.

Would that be something of interest to you?

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```

---

## 2) Same shape — **merge tags** (any ICP + service)

```
{{first_name}},

We can get you in contact with {{icp}} that want {{service}} by using our AI outbound agents.

Would that be something of interest to you?

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```

*(Add a title line under the name in your ESP if you use one, e.g. `CMO` or `{{sender_title}}`.)*

---

## 3) Same offer — **PlusVibe / Smartlead spintax** (`{{random|…}}`)

Full block with spins (no “get” in the middle line): see **`icp-service-ai-outbound-spintax.md`** in this folder — same body pattern with `{{icp}}`, `{{service}}`, and Node Partners footer using `{{sender_first_name}} {{sender_last_name}}`.

---

## 4) Follow-ups (what we actually saved)

**Important:** The original mouthguards / one-liner thread only produced **email 1** (plus spintax files). We did **not** save a separate F1–F3 written only for that one-liner.

In the **same session** you asked for Eric’s full chain; the follow-ups on disk are **Eric’s F1–F3** (value bump → permission → soft breakup), already switched to the **Node Partners** footer and **`{{random|…}}`** where noted. Canonical file: **`eric-nowoslawski-full-sequence.md`**.

Those steps were designed after Eric’s **longer** step-1 bodies (three plays / collapsed / compliment). You can still use **F2 + F3** after the short opener (timing / breakup stay generic). **F1** talks about a “sample list” and “same play” — it fits best if step 1 promised lists or plays; for the pure AI-outbound one-liner you may want a custom F1 or a light rewrite of the first sentence.

**Waits (from that doc):** step 2 = **+3 days** after step 1, step 3 = **+4 days** after step 2, step 4 = **+5 days** after step 3. Subjects for F1–F3: **blank** (same thread).

### F1 — value bump (+3 days)

```
{{first_name}},

{{random|Forgot to mention|Quick add|One more thing}} – {{random|we pulled|we ran|we built}} a sample list of {{icp}} for one of our
{{service}}-style customers last month and got {{random|a 14%|14%|about 14%}} reply rate just
on the first email.

{{random|Worth me running the same play|Want me to run the same play|Should I run the same play}} on {{company_clean}}'s list and {{random|show you the results?|send you what we pulled?|share what that looked like?}}

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```

### F2 — permission close (+4 days after F1)

```
{{first_name}},

{{random|Want me to drop off|Should I drop off|Want me to step away}}, or is this just {{random|bad timing|the wrong week|not a priority right now}}? {{random|Either is totally fine|Either way is fine|Totally fine either way}} – I'll take {{random|one word|a fast yes or no|one line}}.

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```

### F3 — soft breakup (+5 days after F2)

```
{{first_name}},

{{random|Going to assume|I'll assume|I'll chalk this up to}} the timing {{random|isn't right|is off|isn't there}} – I'll loop back {{random|next quarter|in a few months|later this year}}.
If anything changes on the {{icp}} side, {{random|you know where to find me|ping me|I'm easy to find}}.

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```

---

## Other Node-signed copy on disk

| File | What it is |
|------|------------|
| `eric-nowoslawski-full-sequence.md` | Eric sequence, Node Partners sign-off |
| `nowoslawski-icp-service.md` | ICP + service framework + Eric blocks |
| `icp-service-ai-outbound-spintax.md` | AI outbound agents line + `{{random\|…}}` body + optional subject |
| `adrevival-private-management-sequence.md` | AdRevival cold email sequence |

**Paths (easy to open):**  
`c:\Users\caide\outbound-copy-workspace\copy\` — all rows above live there.

---

## Quick paste — **mouthguards plain** with merge tags only

```
{{first_name}},

We can get you in contact with sports teams that want sports mouthguards by using our AI outbound agents.

Would that be something of interest to you?

{{sender_first_name}} {{sender_last_name}}
Node Partners
nodepartners .co
```
